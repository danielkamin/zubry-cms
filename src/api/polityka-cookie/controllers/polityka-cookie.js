'use strict';

/**
 *  polityka-cookie controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::polityka-cookie.polityka-cookie');
