'use strict';

/**
 * dostepne-rozmiary service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dostepne-rozmiary.dostepne-rozmiary');
