'use strict';

/**
 * szczegoly-zamowienia service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::szczegoly-zamowienia.szczegoly-zamowienia');
