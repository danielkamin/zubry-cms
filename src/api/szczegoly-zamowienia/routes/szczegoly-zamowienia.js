'use strict';

/**
 * szczegoly-zamowienia router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::szczegoly-zamowienia.szczegoly-zamowienia');
