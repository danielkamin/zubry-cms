'use strict';

/**
 *  szczegoly-zamowienia controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::szczegoly-zamowienia.szczegoly-zamowienia');
