'use strict';

/**
 * ustawienia-pz-kosz service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ustawienia-pz-kosz.ustawienia-pz-kosz');
